{
  "type": "object",
  "required": ["updated_at", "market_date", "data_phase", "data_state", "source", "scope", "sector_ranking", "sector_stock_ranking", "trading_value_ranking", "volume_surge_ranking", "market_internals", "market_regime", "morning_summary", "key_drivers", "rotation_read", "sector_quality", "scenario_review", "methodology", "data_quality"],
  "properties": {
    "updated_at": {"type": ["string", "null"]},
    "market_date": {"type": ["string", "null"]},
    "data_phase": {"type": "string"},
    "data_state": {"type": "object", "required": ["kind", "label", "message"], "properties": {"kind": {"enum": ["normal", "holiday", "data_error"]}}},
    "source": {"type": "string"},
    "scope": {"type": "string"},
    "sector_ranking": {"type": "array", "items": {"type": "object"}},
    "sector_stock_ranking": {"type": "array", "items": {"type": "object"}},
    "trading_value_ranking": {"type": "array", "items": {"type": "object"}},
    "volume_surge_ranking": {"type": "array", "items": {"type": "object"}},
    "market_internals": {"type": "object"},
    "market_regime": {
      "type": "object",
      "required": ["headline", "headline_reason", "scoreboard", "dimensions", "method_note"],
      "properties": {
        "headline": {"type": "string"},
        "divergence_alert": {"type": ["string", "null"]},
        "scoreboard": {"type": "array", "minItems": 2, "maxItems": 2, "items": {"type": "object", "required": ["label", "value", "unit", "meaning"]}},
        "dimensions": {"type": "array", "items": {"type": "object", "required": ["axis", "label", "status", "reason"]}},
        "method_note": {"type": "string"}
      }
    },
    "morning_summary": {"type": "object", "required": ["stance", "reason", "tailwinds", "headwinds", "focus_sectors"]},
    "key_drivers": {"type": "array", "minItems": 3, "maxItems": 5, "items": {"type": "object", "required": ["driver", "category", "assessment", "status", "reason", "affected_sectors", "selection_factors", "pricing_status", "change_condition"]}},
    "rotation_read": {
      "type": "object",
      "required": ["label", "items", "note"],
      "properties": {
        "label": {"const": "どちらが強い？"},
        "items": {"type": "array", "items": {"type": "object", "required": ["axis", "label", "status", "reason"]}},
        "note": {"type": "string"}
      }
    },
    "sector_quality": {"type": "array", "items": {"type": "object", "required": ["sector", "change_pct", "relative_today_pct", "quality", "attention", "axes", "comment", "coverage_note"], "properties": {"axes": {"type": "object", "required": ["momentum", "activity", "persistence"], "not": {"required": ["breadth"]}}}}},
    "scenario_review": {"type": "object", "required": ["title", "status", "previous_condition", "condition_result", "market_reaction", "unexpected_gap", "gap_reason", "revision", "today_watch", "note"]},
    "methodology": {
      "type": "object",
      "required": ["tier_1_existing_data", "tier_2_light_fetch", "tier_3_new_api_or_ai", "implemented_tier"],
      "properties": {"implemented_tier": {"const": 1}}
    },
    "data_quality": {
      "type": "object",
      "required": ["sector_total", "sector_available", "stock_total", "stock_available", "date_alignment"],
      "properties": {
        "sector_total": {"type": "integer"},
        "sector_available": {"type": "integer"},
        "stock_total": {"type": "integer"},
        "stock_available": {"type": "integer"},
        "date_alignment": {
          "type": "object",
          "required": ["expected_market_date", "sector_market_date", "stock_market_date", "benchmark_market_date", "aligned", "benchmark_aligned"],
          "properties": {
            "expected_market_date": {"type": ["string", "null"]},
            "sector_market_date": {"type": ["string", "null"]},
            "stock_market_date": {"type": ["string", "null"]},
            "benchmark_market_date": {"type": ["string", "null"]},
            "aligned": {"type": "boolean"},
            "benchmark_aligned": {"type": "boolean"}
          }
        }
      }
    }
  },
  "additionalProperties": true
}
